from setuptools import setup

setup(
   name='autoscout',
   version='0.1.0',
   description='Autoscout for football',
   author='Oliver Stanley',
   packages=['autoscout'],
   install_requires=['wheel'],
)